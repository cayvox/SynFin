**/dist/**
**/coverage/**
**/node_modules/**
**/.astro/**
pnpm-lock.yaml
packages/*/src/generated/**

# Hand-written prose (normative spec, ADRs, repo docs) is not auto-formatted;
# we do not impose code formatting on Markdown authored by humans.
**/*.md

# On-ledger (Daml) tree: Daml sources, pinned daml.yaml configs, vendored
# third-party DARs/harness — not governed by the JS/TS Prettier config.
daml/**

# Throwaway research spikes (evidence, not production)
spikes/**

# Vendored design reference artifacts (the orbital/hero source snapshots used to
# match the build pixel-for-pixel) are kept verbatim, not auto-formatted.
apps/web/docs/reference/**

# Release-verify consumer fixtures (resolve against the PUBLISHED packages, not the workspace)
scripts/fixtures/**
