# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Daml build output (the .daml/ dir per package). Vendored DARs in daml/dars/
# are committed deliberately (see daml/dars/README.md).
**/.daml/

# Test / coverage
coverage/

# Editor / OS
.DS_Store
*.log

# Secrets — never commit (see ENGINEERING.md §6). Only .env.example is tracked.
.env
.env.*
!.env.example
