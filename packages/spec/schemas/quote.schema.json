{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "quote.schema.json",
  "title": "Quote",
  "description": "A venue's normalized answer to a QuoteRequest (SPEC §4.3). `receive` is net of the in-receive-asset proportional fee (feeBps) only; flat or differently-denominated costs are carried in the optional `networkFee`, not folded into `receive` (RFC-0005 §1). A firm quote MUST carry a commitment and signature that can be honored on-ledger (SPEC §4.3, §6).",
  "type": "object",
  "properties": {
    "quoteId": {
      "type": "string",
      "minLength": 1,
      "description": "Unique identifier for this quote, set by the venue/adapter; unique within the scope of an intent's quote-gathering round. A RouteLeg.quoteRef resolves to this (SPEC §4.3, §4.4; RFC-0001 Decision C)."
    },
    "venueId": { "$ref": "primitives.schema.json#/$defs/VenueId" },
    "give": {
      "type": "object",
      "description": "Echoes the requested size (SPEC §4.3).",
      "properties": {
        "asset": { "$ref": "primitives.schema.json#/$defs/AssetId" },
        "amount": { "$ref": "primitives.schema.json#/$defs/Decimal" }
      },
      "required": ["asset", "amount"]
    },
    "receive": {
      "type": "object",
      "description": "The offered output, net of the in-receive-asset proportional fee (feeBps) only. Flat or differently-denominated costs are NOT folded in here; they are carried in `networkFee` (SPEC §4.3; RFC-0005 §1).",
      "properties": {
        "asset": { "$ref": "primitives.schema.json#/$defs/AssetId" },
        "amount": { "$ref": "primitives.schema.json#/$defs/Decimal" }
      },
      "required": ["asset", "amount"]
    },
    "feeBps": {
      "type": "integer",
      "minimum": 0,
      "description": "The proportional, in-receive-asset fee already reflected in `receive`, declared for transparency, in basis points (SPEC §4.3; RFC-0005 §1). Flat or differently-denominated costs are carried in `networkFee`, not here."
    },
    "networkFee": {
      "$ref": "primitives.schema.json#/$defs/NetworkFee",
      "description": "OPTIONAL. The flat or gas-like cost the taker bears in addition to `give`, for this quote, in its native asset (RFC-0005 §2). Absent means the venue discloses no separate network fee."
    },
    "sourceKind": {
      "type": "string",
      "enum": ["AMM", "CLOB", "RFQ"],
      "description": "The kind of liquidity source (SPEC §4.3)."
    },
    "settlementMode": {
      "type": "string",
      "enum": ["atomic-allocation", "managed-deposit"],
      "description": "How the venue settles (SPEC §4.3, §5; RFC-0004; ADR-0009). `atomic-allocation`: settles via a CIP-0056 allocation, so this leg CAN be part of a single atomic Daml transaction (SPEC §6). `managed-deposit`: the venue settles via its own deposit/detect/execute flow, so this leg CANNOT be atomically co-settled and is executed via the managed path. Carried on the Quote (alongside `sourceKind`) so a Router operating on quotes can determine each leg's settlement mode."
    },
    "firmness": {
      "type": "string",
      "enum": ["indicative", "firm"],
      "description": "An indicative quote is non-binding; a firm quote is binding and MUST be backed by commitment+signature (SPEC §4.3)."
    },
    "validUntil": {
      "$ref": "primitives.schema.json#/$defs/Timestamp",
      "description": "Consumers MUST reject quotes whose validUntil has passed (SPEC §4.3, §8)."
    },
    "commitment": {
      "$ref": "primitives.schema.json#/$defs/CommitmentRef",
      "description": "REQUIRED if firmness == 'firm' (SPEC §4.3)."
    },
    "signature": {
      "$ref": "primitives.schema.json#/$defs/Signature",
      "description": "REQUIRED if firmness == 'firm' (SPEC §4.3)."
    }
  },
  "required": [
    "quoteId",
    "venueId",
    "give",
    "receive",
    "feeBps",
    "sourceKind",
    "settlementMode",
    "firmness",
    "validUntil"
  ],
  "allOf": [
    {
      "description": "A firm quote MUST carry both a commitment and a signature (SPEC §4.3).",
      "if": {
        "properties": { "firmness": { "const": "firm" } },
        "required": ["firmness"]
      },
      "then": { "required": ["commitment", "signature"] }
    }
  ]
}
